<%= form_with(model: altum, local: true) do |form| %>
  <% if altum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(altum.errors.count, "error") %> prohibited this altum from being saved:</h2>

      <ul>
      <% altum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha, id: :altum_fecha %>
  </div>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad, id: :altum_cantidad %>
  </div>

  <div class="field">
    <%= form.label :plataforma %>
    <%= form.text_field :plataforma, id: :altum_plataforma %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

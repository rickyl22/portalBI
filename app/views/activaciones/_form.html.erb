<%= form_with(model: activacione, local: true) do |form| %>
  <% if activacione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activacione.errors.count, "error") %> prohibited this activacione from being saved:</h2>

      <ul>
      <% activacione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha, id: :activacione_fecha %>
  </div>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad, id: :activacione_cantidad %>
  </div>

  <div class="field">
    <%= form.label :plataforma %>
    <%= form.text_field :plataforma, id: :activacione_plataforma %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

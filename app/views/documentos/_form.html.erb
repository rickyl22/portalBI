<%= form_with(model: documento, local: true) do |form| %>
  <% if documento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(documento.errors.count, "error") %> prohibited this documento from being saved:</h2>

      <ul>
      <% documento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :documento_titulo %>
  </div>

  <div class="field">
    <%= form.label :path %>
    <%= form.text_area :path, id: :documento_path %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
